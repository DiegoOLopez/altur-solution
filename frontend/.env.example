# AuraVoice — Entorno de desarrollo frontend (HackMTY26 / Altur)
# Copia a .env para sobreescribir el valor por defecto.
# Base URL del backend de detección (POST /detect y POST /detect_streaming).
VITE_API_BASE_URL=http://localhost:8000